
services:
  - type: web
    name: insurance-replier-hf-api
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    plan: free
    envVars:
      - key: HF_TOKEN
        sync: false
